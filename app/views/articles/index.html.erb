<%= link_to "Submit Article", new_article_path, class: "btn btn-success btn-sm" %></td>
<h3>News</h3>
<% @allarticles.each do |article| %>
    <hr>
    <h3> <%= article.title %>  </h3>
    <p><i><%= article.updated_at %> </i></p>
    <p> <%= truncate(article.content, :length => 100) %> </p>
    <p> <%= link_to "Read All", article_path(article) %> </p>

<% end %>
